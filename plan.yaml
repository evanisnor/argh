---
# argh — Implementation Plan
# Generated from VISION.md and ARCHITECTURE.md
# Last updated: 2026-03-19

# ─── Testing Policy ──────────────────────────────────────────────────────────
# All code must be unit tested with 100% branch and line coverage.
# Tests are written alongside the implementation as part of each task.
# Each task includes a `testing` field describing the key scenarios to cover.
# Use table-driven tests where multiple input variants exist.
# Mock external boundaries (GitHub API, filesystem, OS notifications) with
# interfaces; test internal logic against fakes or in-memory implementations.

# ─── Task Status Values ───────────────────────────────────────────────────────
# Valid status values: pending | inprogress | done

tasks:

  # ─── Phase 1: Project Foundation ────────────────────────────────────────────

  - id: 1
    title: Initialize Go module and directory structure
    status: done
    depends_on: []
    description: >
      Run `go mod init github.com/evanisnor/argh` targeting Go 1.22+. Create the
      canonical directory layout from ARCHITECTURE.md:
        cmd/argh/main.go
        internal/api/
        internal/ui/
        internal/watches/
        internal/notify/
        internal/config/
        internal/persistence/
        internal/diff/
      Add a stub main.go that prints the version and exits cleanly.
      Add a Brewfile at the repo root.
    references:
      - "ARCHITECTURE.md § Directory Structure"
      - "ARCHITECTURE.md § Technology Stack"
    testing: >
      Verify the module compiles and the stub binary exits with code 0.

  - id: 2
    title: macOS-only runtime guard
    status: done
    depends_on: [1]
    description: >
      At startup, check `runtime.GOOS == "darwin"`. If not, print an informative
      error ("argh v1 is macOS only") and exit with a non-zero status code.
      This is a hard non-goal for v1 per the PRD.
    references:
      - "VISION.md § 1 Non-Goals — Windows or Linux support"
    testing: >
      Table-driven: GOOS=darwin → no error; GOOS=linux → error + non-zero exit.
      Inject GOOS as a parameter rather than reading runtime.GOOS directly so
      the guard is testable without cross-compilation.

  - id: 3
    title: Config loading (internal/config)
    status: done
    depends_on: [1]
    description: >
      Implement config loading from ~/.config/argh/config.yaml using
      gopkg.in/yaml.v3. Provide sane defaults for all fields. Config struct
      should cover:
        - poll_interval (default 10s)
        - notifications: ci_pass, ci_fail, approved, changes_requested,
          review_requested, merged, watch_triggered (all bool)
        - do_not_disturb.schedule (list of windows with days/from/to/all_day)
        - sleep_schedule.poll_interval (default 5m) and windows (same shape)
      Create the config directory (~/.config/argh/) on first run if absent.
      Expose a global Config singleton loaded once at startup.
    references:
      - "VISION.md § 7 Notification System — configurable per-event yaml"
      - "VISION.md § 9.6 Do Not Disturb Mode — scheduled DND yaml"
      - "VISION.md § 9.7 Sleep Schedule — yaml config"
      - "ARCHITECTURE.md § Technology Stack — Config: YAML via gopkg.in/yaml.v3"
    testing: >
      - Missing file → all defaults applied.
      - Partial file → specified fields overridden, rest use defaults.
      - Full valid file → all fields parsed correctly.
      - Malformed YAML → clear error returned.
      - Config dir created if absent (use temp dir in tests).

  # ─── Phase 2: Persistence / Cache Layer ─────────────────────────────────────

  - id: 4
    title: SQLite schema and persistence layer (internal/persistence)
    status: done
    depends_on: [1]
    description: >
      Set up mattn/go-sqlite3. Design and migrate the schema covering:
        - pull_requests: id, repo, number, title, status, ci_state, draft,
          author, created_at, updated_at, last_activity_at, url, global_id
        - reviewers: pr_id, login, state (APPROVED, CHANGES_REQUESTED,
          PENDING, COMMENTED)
        - check_runs: pr_id, name, state, conclusion, url
        - review_threads: pr_id, id, resolved, body, path, line
        - timeline_events: pr_id, event_type, actor, created_at, payload_json
        - watches: id, pr_url, pr_number, repo, trigger_expr, action_expr,
          status (waiting, scheduled, fired, failed), created_at, fired_at
        - session_ids: pr_url, session_id  (reset on restart)
        - rate_limit: singleton row tracking remaining/reset values
        - etags: url, etag, last_modified  (for conditional REST requests)
      Use WAL mode for concurrent reads. Expose typed read/write methods.
      Store database at ~/.local/share/argh/argh.db; create directory on first run.
    references:
      - "ARCHITECTURE.md § High-Level Architecture — DB: SQLite reactive cache"
      - "ARCHITECTURE.md § Data Flow Principles"
      - "VISION.md § 9.1 PR Status Bar Overlay — reads from local SQLite"
    testing: >
      Use an in-memory SQLite DB (:memory:) for all tests.
      - Round-trip write/read for every table.
      - Update existing rows: verify only changed fields update.
      - Schema migration runs cleanly on a fresh DB.
      - WAL mode is enabled after open.
      - Data directory is created when absent.

  - id: 5
    title: Reactive event bus (DB → subscribers)
    status: done
    depends_on: [4]
    description: >
      Implement a lightweight in-process publish/subscribe event bus that the
      persistence layer fires whenever a row changes. Events should carry:
        - event type (PR_UPDATED, CI_CHANGED, REVIEW_CHANGED, WATCH_FIRED, etc.)
        - before and after snapshots of the changed row
      The UI model and Watch Engine both subscribe to this bus. Use Go channels
      or a callback registry. This is the mechanism that makes UI reactive without
      the UI polling the database.
    references:
      - "ARCHITECTURE.md § Data Flow Principles — DB emits events to UI and Watch"
    testing: >
      - Subscribe → publish → subscriber receives event with correct before/after.
      - Multiple subscribers all receive the same event.
      - Unsubscribe → subsequent publishes not delivered.
      - No goroutine leak after bus shutdown.

  # ─── Phase 3: GitHub API Client ─────────────────────────────────────────────

  - id: 6
    title: GitHub authentication via gh CLI (internal/api)
    status: done
    depends_on: [1]
    description: >
      Shell out to `gh auth token` to retrieve the current authenticated user's
      token. Fail fast with a clear error if `gh` is not installed or not
      authenticated. Store token in memory for the lifetime of the process.
      Also fetch the authenticated user's login (`gh api user --jq .login`)
      for use in search queries.
    references:
      - "ARCHITECTURE.md § GitHub API Strategy — Authentication: gh auth token"
    testing: >
      Inject a command executor interface so tests can fake `gh` output.
      - gh returns token → token stored, login fetched.
      - gh not found → clear error.
      - gh not authenticated (non-zero exit) → clear error.
      - gh returns empty token → clear error.

  - id: 7
    title: GraphQL client — fetch My Pull Requests
    status: done
    depends_on: [6, 4]
    description: >
      Use shurcooL/githubv4 to query `search(query: "is:pr is:open author:@me")`
      for all open PRs authored by the current user across all repos. For each PR
      fetch: repo, number, title, state, isDraft, createdAt, updatedAt,
      reviewRequests (requestedReviewers), reviews (nodes: author, state),
      checkSuites → checkRuns (name, status, conclusion), url, id, comments count,
      mergeQueueEntry (to detect merge queued status).
      Write results into the persistence layer. Track last-seen state to detect
      changes and emit events.
    references:
      - "VISION.md § 3.1 My Pull Requests Panel — columns"
      - "ARCHITECTURE.md § GitHub API Strategy — GraphQL Search, GraphQL Query"
    testing: >
      Mock the GraphQL client interface.
      - Successful response → all fields written to DB correctly.
      - New PR appears → PR_UPDATED event emitted.
      - Existing PR changes CI state → CI_CHANGED event emitted.
      - No changes → no events emitted.
      - API error → error returned, DB unchanged.

  - id: 8
    title: GraphQL client — fetch Review Queue
    status: done
    depends_on: [6, 4]
    description: >
      Query `search(query: "is:pr is:open review-requested:@me")` to find PRs
      where the current user is a requested reviewer. Same nested fields as
      task 7. Write into persistence layer. Include author login and per-PR
      timeline events for urgency calculation (last activity timestamp,
      author wait time).
    references:
      - "VISION.md § 3.2 Review Queue Panel — columns and urgency formula"
      - "ARCHITECTURE.md § GitHub API Strategy — GraphQL Search"
    testing: >
      Same mock strategy as task 7.
      - Author wait time correctly derived from timeline events.
      - PRs already in DB from task 7 (authored by self + review requested) are
        not duplicated — each panel reads from its own query result set.

  - id: 9
    title: REST mutations — PR actions
    status: done
    depends_on: [6]
    description: >
      Implement REST action methods via google/go-github for all write operations:
        - approve(repo, number)          → POST /pulls/{number}/reviews
        - requestReview(repo, number, users)
        - postComment(repo, number, body)
        - addLabel / removeLabel
        - mergePR(repo, number, method)  → PUT /pulls/{number}/merge
        - closePR / reopenPR
        - markReadyForReview             → GraphQL mutation (no REST equivalent)
        - convertToDraft                 → GraphQL mutation (no REST equivalent)
        - resolveReviewThread(threadID)  → GraphQL mutation (no REST equivalent)
      Every mutation appends a line to the audit log (task 24).
    references:
      - "VISION.md § 3.6 Interactive Commands"
      - "VISION.md § 9.3 Inline Comment Thread Browser — mark resolved"
      - "ARCHITECTURE.md § GitHub API Strategy — REST API: mutations"
    testing: >
      Mock the REST and GraphQL clients via interfaces.
      - Each mutation calls the correct endpoint with correct parameters.
      - Successful mutation → audit log entry appended.
      - API error → error returned, audit log not written.

  - id: 10
    title: Rate limit tracking and adaptive back-off
    status: done
    depends_on: [7, 8]
    description: >
      On every API response, read x-ratelimit-remaining and x-ratelimit-reset
      headers. Persist values to the rate_limit singleton row in SQLite. Apply
      adaptive back-off logic:
        remaining > 1000  → poll at configured interval
        500–999           → 2× interval
        100–499           → 5× interval
        < 100             → pause polling, emit warning event for status bar
      Expose current rate limit state for the status bar display
      ("API ●●●○ 3,847/5,000").
    references:
      - "ARCHITECTURE.md § Polling & Rate Limits — Adaptive Back-Off"
    testing: >
      Table-driven over all four remaining thresholds (>1000, 500-999, 100-499, <100).
      - Each threshold produces the correct interval multiplier.
      - <100 emits a warning event.
      - Values are persisted to the rate_limit row after each response.

  - id: 11
    title: ETag / conditional request caching for REST endpoints
    status: done
    depends_on: [9]
    description: >
      For REST endpoints used in polling (PR details, check runs), store the
      ETag and Last-Modified response headers in the etags table in SQLite.
      On subsequent requests send If-None-Match / If-Modified-Since headers.
      A 304 response skips writing to the DB and costs 0 primary-limit points.
    references:
      - "ARCHITECTURE.md § Polling & Rate Limits — REST Conditional Requests"
    testing: >
      - First request: no ETag sent, response ETag stored.
      - Second request: ETag header sent; 304 response → DB not written.
      - 200 response with new ETag → DB written, ETag updated.

  # ─── Phase 4: Poll Ticker & Sleep Schedule ───────────────────────────────────

  - id: 12
    title: Poll ticker goroutine
    status: done
    depends_on: [7, 8, 10]
    description: >
      Start a goroutine that fires the GraphQL fetch (tasks 7 & 8) on a
      configurable ticker defaulting to 10s. The ticker interval is dynamically
      adjusted by the rate-limit back-off logic (task 10) and the sleep schedule
      (task 13). Expose a channel to force an immediate poll (used by :reload).
      Gracefully shut down on process exit.
    references:
      - "ARCHITECTURE.md § High-Level Architecture — Poll Ticker"
      - "ARCHITECTURE.md § Chosen Poll Interval: 10 seconds"
    testing: >
      Use a fake clock / injectable ticker.
      - Ticker fires → fetch functions called.
      - Force-poll channel → immediate fetch without waiting for tick.
      - Shutdown signal → goroutine exits cleanly, no leak.
      - Interval changes mid-run (rate-limit back-off) → next tick uses new interval.

  - id: 13
    title: Sleep schedule — reduced polling during off-hours
    status: done
    depends_on: [3, 12]
    description: >
      Read sleep_schedule config (windows + poll_interval). Before each tick,
      check whether the current time falls inside a sleep window. If so, use
      the sleep poll_interval instead of the normal one. Status bar shows
      "💤 sleeping (next poll in Xm)" during sleep windows. Implement :wake
      command to immediately resume normal polling regardless of schedule.
    references:
      - "VISION.md § 9.7 Sleep Schedule"
    testing: >
      Inject a clock interface for time.Now().
      Table-driven over: inside window, outside window, boundary (start/end of window),
      all_day window, multi-day window spanning midnight.
      - Inside window → sleep interval used.
      - Outside window → normal interval used.
      - :wake → normal interval restored immediately regardless of window.

  # ─── Phase 5: TUI Foundation ─────────────────────────────────────────────────

  - id: 14
    title: Bubble Tea root model and layout (internal/ui/model.go)
    status: done
    depends_on: [5]
    description: >
      Initialize the Bubble Tea program. Root model holds references to all
      sub-models (panels, command bar, detail pane). Implement the top-level
      Update() dispatching messages to the correct sub-model. Implement View()
      composing the full terminal layout using lipgloss:
        - Header bar (app name, version, username, global status)
        - My Pull Requests panel
        - Review Queue panel
        - Watches panel (hidden when empty)
        - Detail pane (collapsible, side or bottom)
        - Command bar (pinned to bottom)
      Use lipgloss.HasDarkBackground() for automatic theme selection.
      Subscribe to the event bus (task 5) and translate DB events into
      Bubble Tea commands/messages.
    references:
      - "VISION.md § 4 Wireframe Layout"
      - "ARCHITECTURE.md § UI & Theming"
      - "ARCHITECTURE.md § Directory Structure — ui/model.go"
    testing: >
      Use bubbletea/teatest or drive Update() directly with synthetic messages.
      - DB event → correct Bubble Tea message dispatched to correct sub-model.
      - View() output contains all expected panel regions.
      - Watches panel absent from View() when no watches exist.
      - Detail pane absent from View() when collapsed.

  - id: 15
    title: Global keyboard navigation
    status: done
    depends_on: [14]
    description: >
      Handle global key bindings in the root Update():
        j/↓       move focus down within focused panel
        k/↑       move focus up
        Tab       cycle between panels (My PRs → Review Queue → Watches)
        Enter     expand detail pane for focused PR
        /  or :   focus command bar
        Esc       dismiss overlay or unfocus command bar
        o         open focused PR in browser
        d         show diff for focused PR
        a         approve focused PR (when in Review Queue)
        r         open reviewer picker for focused PR
        ?         toggle help overlay
        q         quit
        R         force reload (:reload)
        D         toggle Do Not Disturb
        p         toggle detail pane
    references:
      - "VISION.md § 5 Interaction Model — Keyboard Navigation"
    testing: >
      Table-driven over every key binding.
      - Each key produces the correct model state change or command.
      - `a` (approve) only fires when focus is in Review Queue, not My PRs.
      - Esc dismisses overlay when one is open; unfocuses command bar otherwise.

  # ─── Phase 6: Panels ─────────────────────────────────────────────────────────

  - id: 16
    title: Local session ID assignment
    status: done
    depends_on: [4, 14]
    description: >
      At startup and on each reload, assign alphabetic session IDs (a, b, c …
      z, aa, ab …) to every PR visible in My PRs and Review Queue, in display
      order. Store mapping in the session_ids table (keyed by PR URL for
      stability). Clear and reassign on :reload. IDs are used as fast command
      bar references and displayed as the first column in both panels.
    references:
      - "VISION.md § 3.1 My Pull Requests — id column"
      - "VISION.md § 5 Local Session IDs"
    testing: >
      - 1 PR → id "a"; 26 PRs → ids "a"–"z"; 27 PRs → "a"–"z", "aa".
      - Reload clears old IDs and reassigns from scratch.
      - PR URL key is stable across reloads (same URL → same ID if order unchanged).
      - session_ids table is cleared on restart.

  - id: 17
    title: My Pull Requests panel (internal/ui/panel_prs.go)
    status: done
    depends_on: [14, 16]
    description: >
      Render the My Pull Requests panel. Read PR rows from the DB. Columns:
        id · 👁 · Repo · # · Title · Status · CI · Reviews · Comments · Age
      Behaviors:
        - Sort by staleness (longest idle time first).
        - Highlight rows with failing CI or blocking reviews.
        - Draft PRs dimmed / prefixed with [draft].
        - Flash animation on any field change (via event bus message).
        - Show 👁 icon when PR has one or more active watches.
        - Show badge count "[N open]" in panel header.
      Status values: draft, open, approved, changes requested, merge queued.
      CI values: ✓ passing, ✗ failing, ⟳ running, — none.
    references:
      - "VISION.md § 3.1 My Pull Requests Panel"
      - "ARCHITECTURE.md § Directory Structure — ui/panel_prs.go"
    testing: >
      - Sorting: PRs with older last_activity_at appear first.
      - Draft PRs rendered with [draft] prefix and dimmed style.
      - Failing CI row has highlight style applied.
      - 👁 icon present when watches exist, absent when none.
      - Badge count matches number of rows.
      - All five status values render correctly.
      - All four CI states render correct symbol.

  - id: 18
    title: Review Queue panel (internal/ui/panel_reviews.go)
    status: done
    depends_on: [14, 16]
    description: >
      Render the Review Queue panel. Read from DB. Columns:
        id · 👁 · Repo · # · Title · Author · CI · Age · Urgency
      Urgency formula (clamped to 1–3):
        Base: <4h → 1, 4–24h → 2, >24h → 3
        CI modifier: failing +1, passing -1, running/none ±0
        Author wait >72h: +1
      Urgency display: ●●● (3), ●●○ (2), ●○○ (1).
      Sort by urgency descending.
      Highlight PRs where user is the last required reviewer.
      Highlight "ready to review" (CI passing, no blockers).
      Show badge count "[N waiting]" in panel header.
    references:
      - "VISION.md § 3.2 Review Queue Panel"
      - "ARCHITECTURE.md § Directory Structure — ui/panel_reviews.go"
    testing: >
      Table-driven urgency formula covering all combinations:
        staleness × CI state × author wait → expected score (clamped 1–3).
      At minimum: base=1+ci_fail+author_wait → clamped to 3 (not 4).
                  base=3+ci_pass → 2 (not 1, since clamped ≥1).
      - Sorted by urgency descending.
      - Last-required-reviewer highlight applied correctly.
      - Ready-to-review highlight (CI passing, no blockers).

  - id: 19
    title: Watches panel (internal/ui/panel_watches.go)
    status: done
    depends_on: [14]
    description: >
      Render the Watches panel as the always-visible third panel. Read watch
      rows from the DB. Columns:
        id · Repo · # · Trigger · Action · Status
      Status values: ⟳ waiting, ⟳ scheduled, ✓ fired, ✗ failed.
      Behaviors:
        - Rows flash briefly when a watch fires.
        - Completed/failed watches remain visible briefly then fade out.
        - Panel is hidden (collapsed) when there are no active watches.
        - Show badge count "[N active]" in panel header.
    references:
      - "VISION.md § 3.4 Watches Panel"
      - "ARCHITECTURE.md § Directory Structure — ui/panel_watches.go"
    testing: >
      - No watches → panel collapsed (not present in View output).
      - Each status value renders correct symbol.
      - WATCH_FIRED event → flash animation triggered on correct row.
      - Badge count reflects active (non-fired, non-failed) watches.

  - id: 20
    title: Detail / Preview pane (internal/ui/panel_detail.go)
    status: done
    depends_on: [14]
    description: >
      Collapsible side or bottom pane for the focused PR. Toggled with Enter
      or `p`. Contains:
        - Full PR description rendered via Glamour (markdown).
        - Check runs list (individual CI job names + states).
        - Review thread summary (open vs resolved count).
        - Active watches for this PR.
        - Recent timeline events (commits, reviews, labels).
      Inline thread navigation: n/N cycles through open review threads.
      Implement "mark resolved" action on a thread (calls resolveReviewThread
      GraphQL mutation via task 9) without opening browser.
      Use Bubbles viewport component for scrollable content.
    references:
      - "VISION.md § 3.3 Detail / Preview Pane"
      - "VISION.md § 9.3 Inline Comment Thread Browser"
      - "ARCHITECTURE.md § Technology Stack — Markdown: Glamour"
    testing: >
      - Collapsed by default; Enter/p toggles open.
      - PR description rendered as markdown (Glamour output non-empty).
      - n/N navigation cycles through open threads only (resolved excluded).
      - Mark resolved calls resolveReviewThread with correct thread ID.
      - Thread marked resolved disappears from navigation cycle.
      - Pane with no threads: n/N is a no-op.

  # ─── Phase 7: Command Bar ────────────────────────────────────────────────────

  - id: 21
    title: Command bar and autocomplete (internal/ui/command_bar.go)
    status: done
    depends_on: [14, 16]
    description: >
      Persistent input bar pinned to the bottom. Activated with / or :
      Features:
        - Fuzzy command autocomplete using sahilm/fuzzy as user types.
        - After command selected, activate PR completion:
            local session ID (a, b …), PR number (#42), or title fragment.
          PR list filtered from visible panels.
        - For :request, @ prefix triggers user autocomplete from repo collaborators
          (fetched via REST API, cached).
        - Tab accepts top suggestion; ↑/↓ navigates suggestion list.
        - History navigation with ↑/↓ when suggestion list not active.
        - Inline help hint showing command signature while typing.
        - Esc unfocuses without executing.
      Use Bubbles textinput component as the base.
    references:
      - "VISION.md § 3.5 Command Bar"
      - "VISION.md § 5 Command Bar Autocomplete Behavior"
      - "ARCHITECTURE.md § Technology Stack — Fuzzy Match: sahilm/fuzzy"
    testing: >
      - `:mer` → suggestions include `:merge`, filtered by fuzzy score.
      - Tab accepts top suggestion.
      - After command, PR list activates: local ID, #number, title fragment all match.
      - `:request` + `@` → collaborator list shown.
      - ↑/↓ cycles history when no suggestion list active.
      - Esc → input cleared, bar unfocused.
      - Help hint updates as command is typed.

  # ─── Phase 8: Interactive Commands ───────────────────────────────────────────

  - id: 22
    title: Implement all interactive commands
    status: done
    depends_on: [21, 9]
    description: >
      Wire up all commands listed in VISION.md §3.6. Each command resolves the
      PR reference (session ID, #number, or title fragment) then calls the
      appropriate REST mutation (task 9) or local action:
        :open [#pr]              → open URL in default browser (open cmd)
        :diff [#pr]              → invoke delta pager with PR diff (task 23)
        :approve [#pr]           → REST: submit approval review
        :review [#pr]            → open inline compose view (body + submit)
        :request [#pr] @user...  → REST: request reviewers
        :ready [#pr]             → GraphQL mutation: mark ready for review
        :draft [#pr]             → GraphQL mutation: convert to draft
        :merge [#pr]             → REST: merge using repo's configured method
        :watch [#pr] …           → delegate to Watch Engine (task 27)
        :close [#pr]             → REST: close PR
        :reopen [#pr]            → REST: reopen PR
        :label [#pr] [label]     → REST: add/remove label
        :comment [#pr]           → open inline editor, post comment
        :dnd [duration]          → toggle DND mode (task 32)
        :wake                    → resume normal polling (task 13)
        :reload                  → force immediate poll
        :help                    → show help overlay (task 40)
        :quit / q                → exit
      All commands are immediately acted upon with no confirmation prompt.
    references:
      - "VISION.md § 3.6 Interactive Commands"
    testing: >
      - PR reference resolution: session ID, #number, and title fragment all
        resolve to the correct PR; unknown reference returns clear error.
      - Each command calls the expected mutation/action with correct arguments.
      - :reload triggers the force-poll channel.
      - :quit sends the Bubble Tea quit command.

  - id: 23
    title: Diff viewer integration (internal/diff)
    status: done
    depends_on: [6]
    description: >
      Implement :diff by fetching the PR diff via GitHub REST API
      (Accept: application/vnd.github.diff) and piping it to the `delta`
      pager subprocess. Suspend the Bubble Tea TUI while delta is running,
      then resume on pager exit. Handle missing delta binary gracefully
      (fall back to raw diff or show install instructions).
    references:
      - "ARCHITECTURE.md § Technology Stack — Diff Viewer: delta"
      - "VISION.md § 3.6 :diff command"
    testing: >
      Inject executor interface for subprocess and HTTP client.
      - delta present → diff fetched, piped to delta, TUI suspended/resumed.
      - delta absent → fallback diff shown with install hint.
      - API error fetching diff → error surfaced to user.

  # ─── Phase 9: Audit Log ──────────────────────────────────────────────────────

  - id: 24
    title: Audit log (internal/audit)
    status: done
    depends_on: [1]
    description: >
      Append an entry to ~/.local/share/argh/audit.log for every action argh
      takes: approve, merge, request, comment, label, watch execution, etc.
      Format: ISO-8601 timestamp, action type, repo, PR number, details.
      Create parent directory on first run. This is called from every mutation
      path (task 9) and every watch action execution (task 28).
    references:
      - "VISION.md § 3.6 — every action recorded in audit log"
      - "VISION.md § 9.5 Audit Log"
    testing: >
      Use a temp directory for log path in tests.
      - Entry appended with correct format (parseable ISO-8601 timestamp).
      - Multiple entries accumulate in order.
      - Log directory created if absent.
      - Concurrent writes do not interleave entries (use file locking or
        a single writer goroutine).

  # ─── Phase 10: Watch Engine ──────────────────────────────────────────────────

  - id: 25
    title: Watch trigger parser
    status: done
    depends_on: [1]
    description: >
      Parse the trigger expression syntax from VISION.md §3.7:
        on:approved+ci         (AND: approved AND ci-pass)
        on:ci-pass,approved    (OR: ci-pass OR approved)
        on:24h-stale           (time-based)
      Supported trigger atoms:
        ci-pass, ci-fail, approved (optionally approved:N for N approvals),
        all-threads-resolved, ready-for-review, label-added:<name>,
        label-removed:<name>, <Nh>-stale.
      Return a structured AST that the Watch Engine can evaluate against
      a PR snapshot from the DB.
    references:
      - "VISION.md § 3.7 Watch — Trigger types, Trigger Syntax"
    testing: >
      Table-driven over all atom types, AND combinator, OR combinator,
      mixed AND+OR, invalid expressions.
      - Valid expressions parse to correct AST.
      - approved:N parses N correctly.
      - <Nh>-stale parses duration correctly.
      - label-added:<name> captures label name.
      - Invalid expression → clear parse error.
      AST evaluation tests (separate from parsing):
      - AND: both conditions true → fires; one false → does not fire.
      - OR: one condition true → fires; both false → does not fire.
      - Time-based: elapsed ≥ duration → fires; elapsed < duration → does not fire.

  - id: 26
    title: Watch action parser
    status: done
    depends_on: [1]
    description: >
      Parse action expressions into typed action structs:
        merge[:method]        (squash, merge, rebase — defaults to repo setting)
        merge:squash
        ready                 (mark draft ready for review)
        request:@user         (request review)
        comment:<text>
        label:<name>
        notify                (desktop notification)
      Actions can be combined with `+`: e.g., `comment:text + notify`.
    references:
      - "VISION.md § 3.7 Watch — Action types"
    testing: >
      Table-driven over all action types and combined expressions.
      - Each valid action parses to correct struct with correct fields.
      - merge with no method → method field is empty (use repo default).
      - merge:squash → method=squash.
      - Combined `comment:text + notify` → two action structs returned.
      - Invalid action → clear parse error.

  - id: 27
    title: Watch management commands
    status: done
    depends_on: [25, 26, 4, 21]
    description: >
      Implement :watch sub-commands wired through the command bar:
        :watch [#pr] <trigger> <action>  → parse trigger + action, write watch
                                           row to DB with status=waiting
        :watch list                      → show all active watches (also shown
                                           in Watches panel)
        :watch cancel [id]               → delete or mark cancelled in DB
      Watches persist across restarts in the watches table in SQLite (keyed by
      PR URL, mapped to ephemeral session IDs at runtime). SQLite is the single
      source of truth; no watches.yaml file is used.
    references:
      - "VISION.md § 3.7 Watch — Queue Management"
      - "ARCHITECTURE.md § High-Level Architecture — DB: SQLite"
    testing: >
      - :watch creates row in DB with status=waiting, correct trigger/action expr.
      - :watch list returns all non-cancelled watches.
      - :watch cancel [id] marks row cancelled; subsequent list excludes it.
      - Invalid trigger expression → error, no DB write.
      - Invalid action expression → error, no DB write.
      - Watch survives process restart (read back from DB on next startup).

  - id: 28
    title: Watch Engine goroutine (internal/watches)
    status: done
    depends_on: [5, 25, 26, 9, 24]
    description: >
      Run an independent goroutine that subscribes to the DB event bus (task 5).
      On each PR_UPDATED event, load all watches for that PR and evaluate each
      trigger against the current PR state snapshot. If a trigger condition is
      met, execute the corresponding action(s) via the API client (task 9),
      update the watch row status to "fired" or "failed", append to audit log
      (task 24), and fire a WATCH_FIRED event on the bus. For time-based triggers
      (Nh-stale), maintain an internal timer per watch. Emit bus events on fire
      so the Watches panel can animate.
    references:
      - "VISION.md § 3.7 Watch — Watch Engine"
      - "ARCHITECTURE.md § High-Level Architecture — Watch Engine goroutine"
    testing: >
      - PR_UPDATED event with matching trigger → action executed, status=fired,
        audit entry written, WATCH_FIRED emitted.
      - PR_UPDATED with non-matching trigger → no action, status unchanged.
      - Action fails (API error) → status=failed, error in audit log.
      - Time-based trigger: inject clock; elapsed ≥ duration → fires.
      - Multiple watches for same PR → each evaluated independently.
      - Goroutine exits cleanly on shutdown signal.

  # ─── Phase 11: Notification System ──────────────────────────────────────────

  - id: 29
    title: In-app visual notifications
    status: done
    depends_on: [14, 5]
    description: >
      Subscribe to DB events (task 5) in the UI model. For each high-signal
      event (CI change, review change, PR state change), trigger:
        - Row flash/highlight animation on the affected row in My PRs or
          Review Queue panel.
        - Badge counter update in panel headers.
        - Status bar update: "● #42 approved by @alice — 10s ago".
      Color coding: green (approved/passing), red (failing/changes requested),
      yellow (pending/waiting), blue (info).
    references:
      - "VISION.md § 7 In-App Visual Notifications"
    testing: >
      - CI_CHANGED event → correct row gains flash style in View output.
      - REVIEW_CHANGED event → status bar text updated with correct PR and actor.
      - Badge counter increments on new PR, decrements on close/merge.
      - Each color code applied to correct event type.

  - id: 30
    title: macOS system notifications (internal/notify)
    status: done
    depends_on: [3, 5]
    description: >
      Implement macOS Notification Center dispatch using gen2brain/beeep or
      deckarep/gosx-notifier (no external runtime dependency). Subscribe to
      DB events. Fire system notifications for:
        - CI: running → passing or failing
        - Review approval received
        - Review with changes requested
        - PR merged or closed
        - You are requested as a reviewer on a new PR
        - Watch action executed
      Respect the per-event config flags (notifications: section of config.yaml).
      Skip all system notifications when DND is active (task 32).
    references:
      - "VISION.md § 7 System Notifications"
      - "ARCHITECTURE.md § Technology Stack — Notifications"
    testing: >
      Inject the OS notification sender as an interface.
      - Each trigger event fires a notification with correct title/body.
      - Config flag false for event type → notification not sent.
      - DND active → no notifications sent regardless of config.
      - CI running→passing fires; CI running→running does not fire.

  - id: 31
    title: Notification deduplication and debouncing
    status: done
    depends_on: [30]
    description: >
      Debounce notification dispatch with a 5-second window: if the same
      notification would fire again within 5s, drop the duplicate.
      Collapse CI flapping (pass→fail→pass within 60s) into a single
      notification. Track in-memory state per (pr_url, event_type) key.
    references:
      - "VISION.md § 7 Notification Deduplication"
    testing: >
      Inject clock interface.
      - Same event within 5s → second notification dropped.
      - Same event after 5s → notification sent.
      - CI pass→fail→pass within 60s → single notification.
      - CI pass→fail→pass after 60s reset → two notifications.

  - id: 32
    title: Do Not Disturb mode (VISION.md §9.6)
    status: done
    depends_on: [3, 30, 14]
    description: >
      Implement DND as a runtime flag in the notifier. When active, suppress
      all system notifications (task 30) while in-app visuals continue normally.
      Toggle with :dnd command or D key. Timed DND: :dnd 2h sets a timer that
      re-enables notifications after the duration. Implement scheduled DND from
      config (do_not_disturb.schedule windows — same shape as sleep_schedule).
      Status bar shows "🔕 DND" when active.
    references:
      - "VISION.md § 9.6 Do Not Disturb Mode"
      - "VISION.md § 3.6 :dnd command"
    testing: >
      Inject clock interface.
      - :dnd toggles DND on; second :dnd toggles off.
      - :dnd 2h → DND active; after 2h elapses → DND deactivated.
      - Scheduled window active → DND on automatically.
      - Scheduled window ends → DND off automatically.
      - DND active → status bar shows "🔕 DND".
      - DND active → system notifications suppressed; in-app visuals unaffected.

  # ─── Phase 12: Additional Features ──────────────────────────────────────────

  - id: 33
    title: "argh --status: tmux/prompt status bar mode"
    status: done
    depends_on: [4]
    description: >
      Add a --status CLI flag. When passed, read the current PR state from the
      SQLite DB (does not start the TUI or poll), print a single condensed line
      to stdout, and exit:
        ↑3 PRs  ✗1 CI  ↓2 review
      Handle gracefully when DB does not exist or is stale (print last-known
      state with a staleness indicator). Intended for embedding in tmux
      status-right or shell prompt via command substitution.
    references:
      - "VISION.md § 9.1 PR Status Bar Overlay"
    testing: >
      - DB with known data → correct counts in output.
      - DB does not exist → graceful output (e.g., "argh: no data").
      - DB stale (last_activity_at old) → staleness indicator in output.
      - Output is a single line ending with newline.

  - id: 34
    title: Smart review assignment suggestions (VISION.md §9.2)
    status: done
    depends_on: [9, 21]
    description: >
      When :request #pr is invoked without explicit @users, show a ranked
      suggestion list using up to three signals (all optional, degrade gracefully):
        1. git blame on local repo (if available) — who owns most changed lines.
        2. GitHub API: who reviewed similar PRs recently.
        3. GitHub API: CODEOWNERS rules for changed files.
      Combine signals into a ranked list, de-duplicate. User selects from the
      list before the request is sent.
    references:
      - "VISION.md § 9.2 Smart Review Assignment"
    testing: >
      Mock all three signals independently.
      - All three signals available → merged ranked list, no duplicates.
      - git blame unavailable → ranking uses remaining two signals.
      - All signals unavailable → empty suggestion list, command still works.
      - De-duplication: same login from multiple signals appears once.

  - id: 35
    title: Per-repo configuration (.argh.yaml)
    status: done
    depends_on: [3]
    description: >
      When argh is run inside a git repo (or when a PR action targets a repo),
      look for .argh.yaml in the repo root (fetched via GitHub API, not local fs).
      If found, parse and apply overrides:
        - default_reviewers: list of logins
        - label_conventions: map of shorthand → full label name
        - merge_strategy: squash | merge | rebase
      Repo-level config takes precedence over global config for the relevant repo.
    references:
      - "VISION.md § 9.4 Per-Repo Configuration"
    testing: >
      Mock the GitHub API file fetch.
      - .argh.yaml present → overrides applied for that repo.
      - .argh.yaml absent → global config used unchanged.
      - .argh.yaml malformed → error logged, global config used as fallback.
      - Repo-level merge_strategy overrides global for merge command on that repo.

  # ─── Phase 13: Help Overlay ──────────────────────────────────────────────────

  - id: 40
    title: Help overlay (internal/ui/overlay_help.go)
    status: done
    depends_on: [14]
    description: >
      Implement a full-screen overlay showing all available commands and keyboard
      shortcuts as a reference card. Toggled with `?` key or `:help` command.
      Dismissed with Esc or `?` again. Rendered on top of the normal layout
      using lipgloss — underlying panels are still visible but dimmed.
      Content sections:
        - Keyboard shortcuts (j/k/Tab/Enter/o/d/a/r/q/R/D/p/?)
        - Command reference (:open, :diff, :approve, :review, :request,
          :ready, :draft, :merge, :watch, :close, :reopen, :label, :comment,
          :dnd, :wake, :reload, :help, :quit)
        - Watch trigger syntax examples
        - Watch action syntax examples
    references:
      - "VISION.md § 3.6 Interactive Commands — :help"
      - "VISION.md § 5 Keyboard Navigation — ?"
    testing: >
      - ? key → overlay visible in View output.
      - Esc → overlay dismissed, underlying layout restored.
      - Second ? → overlay dismissed.
      - :help → overlay visible.
      - Overlay content contains all keyboard shortcuts and commands.

  # ─── Phase 14: Distribution & CI/CD ─────────────────────────────────────────

  - id: 36
    title: goreleaser configuration and cross-compilation
    status: done
    depends_on: [1]
    description: >
      Add .goreleaser.yaml targeting macOS arm64 and amd64. Embed version from
      git tag at build time (ldflags). Configure GitHub Releases artifact upload.
      Include SHA256 checksums. Validate with `goreleaser check`.
    references:
      - "ARCHITECTURE.md § Build & Release — goreleaser"
    testing: >
      Run `goreleaser check` in CI to validate config. No unit tests required.

  - id: 37
    title: GitHub Actions CI/CD workflow
    status: done
    depends_on: [36]
    description: >
      Create .github/workflows/ with:
        - ci.yml: runs on push/PR — `go test -race -coverprofile=coverage.out ./...`,
          `go vet`, staticcheck. Fail if coverage drops below 100%.
        - release.yml: triggers on tag push (v*) — runs goreleaser, uploads
          binaries to GitHub Releases.
      Include version number from tag in the binary via ldflags.
    references:
      - "ARCHITECTURE.md § Build & Release — GitHub Actions"
    testing: >
      Workflow correctness is validated by GitHub Actions execution. No separate
      unit tests. Ensure `go test -race` is used to catch data races.

  - id: 38
    title: Homebrew tap formula
    status: done
    depends_on: [37]
    description: >
      After a release is published, update the Homebrew formula in the
      evanisnor/homebrew-tap repository
      (~/Code/github.com/evanisnor/homebrew-tap). Formula should:
        - Reference the GitHub Release binary URLs for arm64 and amd64.
        - Declare runtime dependencies on `gh` and `git-delta`.
        - Include a post-install caveats message guiding through `gh auth login`.
      Automate formula SHA256 update as a step in the release workflow.
    references:
      - "ARCHITECTURE.md § Distribution & Dependencies — Homebrew Formula"
      - "ARCHITECTURE.md § Installation Flow"
    testing: >
      Run `brew audit --strict` against the formula. No unit tests required.

  - id: 41
    title: Wire up main() — full application launch
    status: done
    depends_on: [14, 12, 30, 28, 24, 23, 27, 33]
    description: >
      Replace the version-printing stub in main.go with a full application launch:
      authenticate via gh CLI, open the SQLite DB, create the event bus, wire up
      the poller, DND manager, system notifier, watch engine, UI panels, and Bubble
      Tea root model. Also: add ForcePoll() to Poller, WithDNDToggler() to Model,
      Defaults() to config, wire the CommandExecutor into the CommandBar, and handle
      CommandResultMsg/CommandComposeMsg in the root model.
    references:
      - "ARCHITECTURE.md — wiring diagram"
    testing: >
      Inject tuiDeps stubs to exercise every error branch (auth failure, config
      failure, DB open failure, audit log path failure) and the happy path. Add
      tests for ForcePoll(), WithDNDToggler(), Defaults(), command bar executor
      dispatch on Enter, and model handling of CommandResultMsg and CommandComposeMsg.

  - id: 39
    title: Brewfile
    status: done
    depends_on: [1]
    description: >
      Create Brewfile at the repo root declaring runtime dependencies:
        tap "evanisnor/tap"
        brew "gh"
        brew "dandavison/delta/git-delta"
        brew "evanisnor/tap/argh"   # once published
      This allows `brew bundle` to install everything in one step.
    references:
      - "ARCHITECTURE.md § Distribution & Dependencies — Brewfile"
    testing: >
      No unit tests required. Validated by `brew bundle check`.

  # ─── Phase 9: Full-Screen TUI Layout ─────────────────────────────────────────

  - id: 42
    title: Add ResizeMsg and handle tea.WindowSizeMsg in root model
    status: done
    depends_on: [41]
    description: >
      In internal/ui/model.go:
        - Define ResizeMsg{Width, Height int} message type.
        - Add width and height int fields to Model.
        - Handle tea.WindowSizeMsg in Update(): store dims, then propagate
          ResizeMsg to all sub-models (myPRs, reviewQueue, watches, detailPane,
          commandBar) with their allocated dimensions (see task 43 for how height
          is distributed).
      No visual change yet — dimensions are stored but not yet applied in View().
    references:
      - "VISION.md § 4. Wireframe Layout"
    testing: >
      Add a test that sends tea.WindowSizeMsg{Width: 120, Height: 40} to the root
      model and verifies width and height are stored. Verify ResizeMsg is forwarded
      to sub-models (use a spy SubModel that records received messages). Table-driven
      across a few width/height values including 0x0 (before first resize).

  - id: 43
    title: Apply Width and Height constraints in root View()
    status: done
    depends_on: [42]
    description: >
      In internal/ui/model.go View() and its helpers:
        - headerView(): apply Width(m.width) to the header lipgloss style so the
          header spans the full terminal width.
        - commandBarView(): apply Width(m.width) to the command bar style.
        - panelView(): apply Width(m.width - 2) (inner, accounting for NormalBorder
          left+right chars) and Height(panelHeight - 2) (inner) to each panel border
          style. panelHeight is computed as (availableHeight / numVisiblePanels)
          where availableHeight = m.height - 1 (header) - 1 (command bar).
        - Add numVisiblePanels() and panelContentHeight() helpers.
        - When m.width == 0 (before first WindowSizeMsg) skip Width/Height
          constraints and fall back to natural sizing.
    references:
      - "VISION.md § 4. Wireframe Layout"
    testing: >
      Add tests that set width/height on the model (via tea.WindowSizeMsg) then
      call View() and check that the rendered string contains lines of exactly the
      expected width. Table-driven across: 0x0 (natural sizing), 80x24, 120x40.
      Verify each visible panel is height-constrained. Verify header and command
      bar span full width.

  - id: 44
    title: Handle ResizeMsg in panels; truncate row content to allocated width
    status: done
    depends_on: [42]
    description: >
      In internal/ui/panel_prs.go, panel_reviews.go, panel_watches.go:
        - Add width int field to each panel struct.
        - Handle ResizeMsg in Update(): store m.Width.
        - In renderRow(), truncate the title (or full row text) to fit within
          the allocated width so long titles do not cause row wrapping.
          Truncated titles get a trailing "…" character.
        - When width == 0, skip truncation (natural sizing fallback).
    references:
      - "VISION.md § 4. Wireframe Layout"
    testing: >
      Table-driven tests: send ResizeMsg with various widths (0, 40, 80, 120),
      then call View() and verify no row in the output exceeds the allocated width.
      Include cases with very long PR titles and very short widths.

  - id: 45
    title: Reorder CommandBar.View() so suggestions appear above the input line
    status: done
    depends_on: [42]
    description: >
      In internal/ui/command_bar.go, change View() so that when the bar is focused
      and suggestions exist, the suggestion lines are rendered BEFORE the textinput
      line. Since the command bar is the last rendered element (bottom of screen),
      this causes suggestions to appear above the input rather than below it
      (which would push them off the bottom of the terminal).
      Update the ">" cursor indicator to remain on the focused suggestion line.
    references:
      - "VISION.md § 3.5 Command Bar"
    testing: >
      Add tests that focus the command bar, set suggestions, and call View(). Verify
      that suggestion lines appear before the input line in the returned string.
      Test with 0, 1, and maxSuggestions suggestions. Test unfocused state is unchanged.

  - id: 46
    title: Overlay suggestion pane above command bar without pushing panels off-screen
    status: done
    depends_on: [45]
    description: >
      CommandBar.View() previously returned suggestion lines prepended to the input
      line, causing the total layout height to exceed the terminal when suggestions
      were shown. Fix by decoupling suggestions from the vertical layout stack:
      (1) CommandBar.View() returns only the single input line + hint.
      (2) New HasSuggestions() bool and SuggestionsView() string methods on CommandBar.
      (3) New overlayAbove(base, overlay string) string helper in overlay_suggestions.go
      replaces panel lines just above the command bar with suggestion content,
      keeping the total layout height fixed at terminal height.
      (4) Model.View() calls overlayAbove() after building the normal layout when the
      command bar has suggestions, via the new CommandBarOverlay interface and
      commandBarSuggestionsView() helper (which applies the CommandBar theme style).
    references:
      - "internal/ui/command_bar.go"
      - "internal/ui/overlay_suggestions.go"
      - "internal/ui/model.go"
    testing: >
      Updated command_bar_test.go: suggestions verified in SuggestionsView() not View();
      new HasSuggestions, SuggestionsView, and no-trailing-newline tests.
      New overlay_suggestions_test.go: table-driven tests for overlayAbove (empty,
      fits, clamped, line count preserved).
      New model_test.go tests: stubCommandBarOverlay exercises overlay path in View()
      and commandBarSuggestionsView(). 100% statement coverage maintained.

  - id: 47
    title: Detail pane as floating modal overlay
    status: done
    depends_on: [46]
    description: >
      The detail pane was appended as a stacked section below the panels, causing
      them to compress when it opened. Replace this with a centred floating modal
      that overlays the panels without affecting their layout.
      (1) New overlayModal(base, modal string, termWidth, termHeight int) string in
      overlay_detail.go: dims background lines per-line using lipgloss.Faint, centres
      the modal with lipgloss.Place, and replaces only the rows occupied by the modal.
      Falls back to stacking when dimensions are zero (pre-WindowSizeMsg).
      (2) Model.View() calls overlayModal() after building the normal layout when
      detailOpen is true, instead of appending to the sections slice.
      (3) detailPaneView() uses a rounded accent border sized at 75% of terminal
      dimensions (width*3/4, height*3/4).
      (4) propagateResize() sends modal-sized dimensions (75% minus border overhead)
      to the detail pane rather than full terminal dimensions.
      (5) Remove DetailPane.visible and Toggle() — visibility is controlled entirely
      by Model.detailOpen. Add ResizeMsg handling to resize the viewport.
    references:
      - "internal/ui/overlay_detail.go"
      - "internal/ui/model.go"
      - "internal/ui/panel_detail.go"
    testing: >
      New overlay_detail_test.go: table-driven tests for overlayModal covering
      zero-dimension fallback, modal content present, line count preserved,
      background lines retain base content, tall modal clamped to base.
      Updated panel_detail_test.go: removed Toggle/visible tests; removed
      p.visible = true setup lines; added TestDetailPane_ResizeMsg.
      Updated model_test.go: existing DETAIL visibility tests continue to pass
      via the zero-dimension fallback path. 100% statement coverage maintained.

  - id: 48
    title: Add SelectedPR() accessor to both PR panels
    status: done
    depends_on: [47]
    description: >
      Add SelectedPR() *persistence.PullRequest method to MyPRsPanel
      (panel_prs.go) and ReviewQueuePanel (panel_reviews.go). Returns the PR
      at p.rows[p.cursor].pr, or nil when the panel has no rows.
    references:
      - "internal/ui/panel_prs.go"
      - "internal/ui/panel_reviews.go"
    testing: >
      Table-driven tests in panel_prs_test.go and panel_reviews_test.go:
      empty panel returns nil; single row returns that PR; after MoveFocusMsg
      the cursor row is returned.

  - id: 49
    title: Guard detail modal on PR selection; populate detail pane data
    status: done
    depends_on: [48]
    description: >
      Fix two bugs: (1) Enter/p opened the detail modal from any panel
      (including Watches, which has no PRs); (2) the detail pane always showed
      blank content because PRFocusedMsg was never sent.
      Changes:
        - Add PRSelector interface (SelectedPR() *persistence.PullRequest) to model.go.
        - Add PRDetailReader interface (ListCheckRuns, ListReviewThreads,
          ListWatches, ListTimelineEvents) to model.go.
        - Add detailReader PRDetailReader field to Model; expose via WithDetailReader().
        - Fix Enter/p key handler: only open modal when focused panel implements
          PRSelector AND SelectedPR() returns non-nil; send PRFocusedMsg with
          fetched detail data on open.
        - After dispatching MoveFocusMsg (j/k/↑/↓), if detailOpen is true,
          call refreshDetailForCursor() to update the detail pane with the new
          cursor PR.
        - Add focusedPRSelector(), tryOpenDetail(), refreshDetailForCursor(),
          and buildPRFocusedMsg() helpers.
    references:
      - "internal/ui/model.go"
    testing: >
      New model_test.go tests: stubPRSelector and stubPRDetailReader test
      doubles; Enter on Watches/empty panel is a no-op; Enter on ReviewQueue
      with PR opens modal; PRFocusedMsg carries check runs/threads/watches/
      timeline from reader; j/k while open sends updated PRFocusedMsg;
      j/k while closed does not; j with nil SelectedPR does not send msg;
      j on Watches panel (no PRSelector) does not send msg; WithDetailReader
      field test. 100% coverage maintained.

  - id: 50
    title: "Help overlay: scrollable centered modal"
    status: done
    description: >
      The help overlay is currently rendered by appending its content below the
      dimmed normal view (JoinVertical), with no fixed dimensions or scrolling.
      This causes it to render off-screen on typical terminals.

      Changes:
        - overlay_help.go: rename renderHelpOverlay(theme) →
          renderHelpContent(theme) (returns raw scrollable text, no border).
          Add helpModalView(vp viewport.Model, theme Theme, width, height int)
          that wraps vp.View() in a rounded border constrained to the supplied
          dimensions (same style as the detail modal).
        - model.go: add helpViewport viewport.Model field. In New(), initialise
          it with viewport.New(80, 20). In the WindowSizeMsg handler, resize
          the viewport to (width*3/4 - 4, height*3/4 - 4) and call
          SetContent(renderHelpContent(m.theme)). In handleKey, when
          helpVisible is true, route j/↓/k/↑/pgdown/pgup to
          m.helpViewport.Update(msg) and return early; keep ? and Esc to
          dismiss. In View(), replace JoinVertical(dimBackground, overlay)
          with overlayModal(normal, helpModalView(...), m.width, m.height).
    references:
      - "internal/ui/overlay_help.go"
      - "internal/ui/model.go"
    testing: >
      Update TestRenderHelpOverlay_* to call renamed renderHelpContent.
      Add tests: j scrolls viewport down; k scrolls up; pgdown pages down;
      modal dimensions respect terminal size; overlay uses overlayModal
      (content appears centred, not below normal layout). Retain all existing
      content-assertion tests. 100% coverage maintained.

  - id: 51
    title: "Fix: unknown command error when entering commands via : or /"
    status: done
    description: >
      ParseCommand now strips any leading ":" or "/" from the parsed command
      name before dispatching. Execute switch cases updated to bare names
      (e.g. "quit", "reload", "help"). Suggestions and help modal display
      are unaffected — commandList names retain their ":" prefix.
    implementation: >
      commands.go: ParseCommand strips leading ":" or "/" from parts[0].
      Execute: all case labels changed from ":cmd" to "cmd".
      commands_test.go: TestParseCommand updated; bare-name and /slash rows
      added. TestExecute tests updated to call Execute with bare names.
      command_bar_test.go: dispatcher assertion updated to expect bare name.

  # ─── Phase 15: Replace gh CLI auth with PAT ─────────────────────────────────

  - id: 52
    title: "Extend config.Filesystem with WriteFile and Remove"
    status: done
    depends_on: [3]
    description: >
      Add WriteFile(path string, data []byte, perm os.FileMode) error and
      Remove(path string) error to the Filesystem interface and OSFilesystem.
      Update fakeFS in config_test.go. Add tests for the new OSFilesystem methods.
    testing: >
      Add WriteFile and Remove methods to fakeFS. Add test for OSFilesystem.WriteFile
      (write to temp dir, read back). Add test for OSFilesystem.Remove.

  - id: 53
    title: "Add PAT storage functions to config package"
    status: done
    depends_on: [52]
    description: >
      New files: internal/config/token.go and internal/config/token_test.go.
      Export ConfigDirPath. Add ErrTokenNotFound sentinel. Add LoadToken, SaveToken,
      DeleteToken functions using the Filesystem interface.
    testing: >
      Table-driven tests: token found, not found (ErrTokenNotFound), empty token,
      save + re-read, save with MkdirAll error, save with WriteFile error,
      UserConfigDir error, delete token, ConfigDirPath correctness.

  - id: 54
    title: "Add TokenVerifier and rewrite Authenticate"
    status: done
    depends_on: [53]
    description: >
      Full rewrite of internal/api/auth.go. Remove CommandExecutor interface.
      Add TokenVerifier interface with Verify(ctx, token) (login, error).
      Add GitHubTokenVerifier using google/go-github oauth2 client.
      Rewrite Authenticate to use config.LoadToken + verifier.Verify.
      Update testing.go: remove StubCommandExecutor, add StubTokenVerifier.
    testing: >
      Token found + verification succeeds, token not found returns ErrTokenNotFound,
      token empty, verification fails, verification returns empty login.

  - id: 55
    title: "Delete CommandExecutor / OSCommandExecutor"
    status: done
    depends_on: [54]
    description: >
      Delete internal/api/executor.go and internal/api/executor_test.go.
      These types are only used by the old Authenticate which is replaced in task 54.

  - id: 56
    title: "Create the setup modal"
    status: done
    depends_on: [54]
    description: >
      New files: internal/ui/setup.go and internal/ui/setup_test.go.
      Standalone Bubble Tea model that runs before the main TUI. Masked text input
      for PAT, verify button, quit button. RunSetup top-level function with
      injected dependencies.
    testing: >
      Drive Update directly. Initial state, type chars, press s with chars,
      verifyResultMsg success/failure, q with empty/non-empty input, esc/ctrl+c,
      WindowSizeMsg, View renders correctly, RunSetup with stubbed RunProgram.

  - id: 57
    title: "Wire PAT auth into main.go"
    status: done
    depends_on: [55, 56]
    description: >
      Update tuiDeps with runSetup, saveToken, deleteToken fields.
      Update productionDeps. Rewrite auth flow in runTUI to handle
      ErrTokenNotFound by showing setup modal, saving token, and re-authenticating.
      Update main_test.go with new test cases.
    testing: >
      TestRunTUI_TokenNotFound_SetupSucceeds, SetupQuit, SetupError, SaveError,
      ReauthFails, InvalidToken_RepromptsSetup. Update existing deps tests.

  - id: 58
    title: "Remove gh dependency from Brewfile and docs"
    status: done
    depends_on: [57]
    description: >
      Remove brew "gh" from Brewfile. Update CLAUDE.md runtime dependencies
      section. Update ARCHITECTURE.md auth references.
    testing: >
      No unit tests. Verify no references to gh CLI remain in Go source.

  # ─── Phase 16: SSO Support & OAuth Device Flow ─────────────────────────────

  - id: 59
    title: "Token type metadata in config package"
    status: done
    depends_on: [58]
    description: >
      Add a token_type file alongside the existing token file in ~/.config/argh/.
      New TokenType string type with constants TokenTypePAT = "pat" and
      TokenTypeOAuth = "oauth". LoadTokenType(fs Filesystem) reads
      ~/.config/argh/token_type; returns TokenTypePAT if file missing.
      SaveTokenType(fs Filesystem, tt TokenType) writes with 0600 perms.
    testing: >
      Table-driven: file present with each value, file missing (defaults to PAT),
      empty file, round-trip save+load, filesystem errors.

  - id: 60
    title: "OAuth Device Flow client"
    status: done
    depends_on: [58]
    description: >
      Implement RFC 8628 device authorization grant for GitHub.
      DeviceFlowClient interface with RequestCode and PollToken methods.
      GitHubDeviceFlowClient accepts HTTPDoer interface and BaseURL for testability.
      Sleeper interface to avoid real time.Sleep in tests.
    testing: >
      Table-driven with httptest.NewServer: success, HTTP errors, invalid JSON,
      poll with pending then success, slow_down increases interval, expired/denied
      errors, cancelled context.

  - id: 61
    title: "SSORequired event type"
    status: done
    depends_on: [58]
    description: >
      Add constant SSORequired EventType = "SSO_REQUIRED" to eventbus/bus.go.
    testing: >
      No new tests needed — existing bus tests prove pub/sub works for any EventType.

  - id: 62
    title: "SSO-aware HTTP round tripper"
    status: done
    depends_on: [61]
    description: >
      SSOInfo struct with OrgName and AuthorizationURL. SSOObserver interface
      with OnSSORequired(info SSOInfo). NewSSOTransport wraps base
      http.RoundTripper, inspects 403 + X-GitHub-SSO header. BusSSOObserver
      publishes SSORequired events to bus.
    testing: >
      Table-driven: 200 (no call), 403 without header (no call), 403 with valid
      header (observer called), malformed header (no crash), transport error.
      BusSSOObserver publishes correct event.

  - id: 63
    title: "Handle SSORequired event in UI model"
    status: done
    depends_on: [61, 62]
    description: >
      Add SSORequired case to handleDBEvent and notifColor in model.go.
      Status bar shows SSO required message with yellow color.
    testing: >
      SSORequired event updates status bar text and color; non-SSOInfo payload
      is a no-op.

  - id: 64
    title: "OAuth client ID in config"
    status: done
    depends_on: [58]
    description: >
      Add OAuthConfig struct with ClientID field to Config. Set default ClientID
      in defaults(). Users can override in config.yaml for GHE.
    testing: >
      Missing oauth section uses default; custom client_id overrides; existing
      tests unaffected.

  - id: 65
    title: "Redesign setup modal with auth method selection"
    status: done
    depends_on: [59, 60, 64]
    description: >
      Refactor setup.go into multi-screen flow: method selection screen,
      device flow screen, and existing PAT entry screen. Updated SetupDeps
      with DeviceFlow, OpenBrowser, ClientID, Scopes. SetupResult struct
      with Token, TokenType, Quit.
    testing: >
      Method selection renders; g transitions to device flow; p transitions to
      PAT entry; device code/token messages; q/esc/ctrl+c quit from all screens;
      SetupResult carries correct TokenType.

  - id: 66
    title: "Device flow test helpers"
    status: done
    depends_on: [60]
    description: >
      Add StubDeviceFlowClient to internal/api/testing.go with RequestCodeFunc
      and PollTokenFunc fields.
    testing: >
      No separate tests — used by setup_test.go.

  - id: 67
    title: "Wire OAuth device flow and SSO transport into main.go"
    status: done
    depends_on: [62, 63, 65, 66]
    description: >
      Update tuiDeps runSetup signature to return SetupResult. Add saveTokenType
      field. Wire GitHubDeviceFlowClient, BusSSOObserver, SSOTransport into
      productionDeps and runTUI. Save both token and token type after setup.
    testing: >
      Update happyDeps with new fields; test OAuth setup path saves token +
      token type; test SSO transport is wired; all existing tests updated.

  # ─── Phase 17: OAuth Device Flow Fix & Error UX ─────────────────────────────

  - id: 68
    title: "Replace placeholder OAuth client ID"
    status: done
    depends_on: [64]
    description: >
      Replace "Ov23liYOUR_CLIENT_ID" in internal/config/config.go with the real
      client ID from the registered GitHub OAuth App.
    testing: >
      Existing config tests pass unchanged. Manual verification only.

  - id: 69
    title: "Improve device flow error messages and add debug logging"
    status: done
    depends_on: [60]
    description: >
      Add a specific 404 check in RequestCode that returns a user-friendly error
      ("GitHub OAuth app not found — verify oauth.client_id in config"). Add
      slog.Error calls at error return points in RequestCode and PollToken so
      failures are captured in the debug log.
    testing: >
      Add a 404 table case to deviceflow_test.go that asserts the new error message.
      Existing tests pass unchanged.

  # ─── Phase 18: SSO Authorization UX ──────────────────────────────────────────

  - id: 70
    title: "Post-device-flow SSO authorization screen"
    status: done
    depends_on: [65]
    description: >
      After the device flow token is obtained, show a new screenSSO screen before
      completing setup. This guides users to authorize SSO orgs since GraphQL search
      silently excludes SSO-protected repos. Add ssoURL field to SetupModel, viewSSO
      method, and key handling for o/s/q/esc/ctrl+c on the SSO screen.
    testing: >
      Update TestSetup_DeviceFlow_RequestCodeSuccess to verify screenSSO transition.
      New tests: SSO view contains URL, o opens browser, s/q skip, esc/ctrl+c set
      done (not quit), nil browser safety, unknown keys ignored, render with dimensions.

  - id: 71
    title: "Auto-open browser on SSO 403 with per-org dedup"
    status: done
    depends_on: [62]
    description: >
      Add BrowserSSOObserver that wraps BusSSOObserver behavior: always publishes
      to bus, then opens browser once per org per session. Wire into main.go replacing
      the plain BusSSOObserver.
    testing: >
      First org call opens browser + publishes; same org twice opens once; different
      orgs open for each; nil OpenBrowser no panic; browser error ignored.

  # ─── Phase 19: gh CLI Backend Support ──────────────────────────────────────

  - id: 82
    title: "Export intermediate types and derivation helpers"
    status: done
    depends_on: []
    description: >
      Export the currently-private types and functions that both backends need:
      checkRunData → CheckRunData, reviewData → ReviewData, commitData → CommitData,
      derivePRStatus → DerivePRStatus, deriveCIState → DeriveCIState, prsEqual → PRsEqual.
      Update all internal references.
    testing: >
      All existing tests pass unchanged (pure rename).

  - id: 83
    title: "Extract persist+publish into shared helper functions"
    status: done
    depends_on: [82]
    description: >
      Extract persist-and-publish logic into package-level functions:
      PersistPR(store, bus, pr, runs, reviews) and PersistRQPR(store, bus, pr, runs, reviews, commits).
      Update both existing fetchers to delegate to these functions.
    testing: >
      Move persist-related assertions into persist_test.go. Verify: new PR event,
      changed PR event, CI changed event, unchanged PR no event, upsert errors.

  - id: 84
    title: "Add TokenTypeGHCLI constant"
    status: done
    depends_on: []
    description: >
      Add TokenTypeGHCLI TokenType = "ghcli" to internal/config/token.go.
    testing: >
      Test round-trip: save TokenTypeGHCLI, load it back, verify value.

  - id: 85
    title: "Create internal/ghcli/runner.go — CommandRunner interface"
    status: done
    depends_on: [84]
    description: >
      New package internal/ghcli/. Define CommandRunner interface with Run method.
      Production ExecRunner uses exec.CommandContext("gh", args...).
      CheckInstalled verifies gh is in PATH.
      Create testing.go with StubCommandRunner.
    testing: >
      Test StubCommandRunner records calls. Test CheckInstalled with stub lookup.

  - id: 86
    title: "Implement GHCLIAuthVerifier"
    status: done
    depends_on: [85]
    description: >
      Implements api.TokenVerifier. Runs gh auth status --hostname github.com
      and parses output to extract the logged-in username. Token parameter ignored.
    testing: >
      Table-driven with StubCommandRunner: successful auth, not authenticated,
      gh not found, malformed output.

  - id: 87
    title: "Implement GHCLIMyPRsFetcher"
    status: done
    depends_on: [83, 85]
    description: >
      Implements Fetcher interface. Runs gh search prs for author's open PRs.
      Parses JSON into CheckRunData/ReviewData, builds persistence.PullRequest
      rows using DerivePRStatus/DeriveCIState, then calls PersistPR.
    testing: >
      Table-driven with stub runner: successful parse, empty result, command error,
      malformed JSON, persist error propagation, event publishing.

  - id: 88
    title: "Implement GHCLIReviewQueueFetcher"
    status: done
    depends_on: [83, 85, 87]
    description: >
      Implements Fetcher interface. Runs gh search prs for review-requested PRs.
      Same pattern as Task 87, with additional commit extraction. Calls PersistRQPR.
    testing: >
      Same pattern as Task 87 plus commit timeline events.

  - id: 89
    title: "Implement GHCLIMutator"
    status: done
    depends_on: [85]
    description: >
      Implements ui.PRMutator (10 methods), watches.ActionExecutor (5 methods),
      and ui.ThreadResolver (1 method) through one struct.
    testing: >
      Table-driven for each method: success, command error, invalid repo format,
      audit logging. Test merge method flag mapping.

  - id: 90
    title: "Implement GHCLIDiffFetcher"
    status: done
    depends_on: [85]
    description: >
      Implements diff.DiffFetcher. Runs gh pr diff. URL parameter parsed to
      extract owner/repo and PR number. Token parameter ignored.
    testing: >
      Success returns diff bytes, error propagation, URL parsing.

  - id: 91
    title: "Implement FixedRateLimitReader"
    status: done
    depends_on: []
    description: >
      Implements api.RateLimitReader. CurrentState() always returns full quota
      (Remaining: 5000, Limit: 5000).
    testing: >
      Verify CurrentState() returns full quota.

  - id: 92
    title: "Add gh CLI option to setup flow"
    status: done
    depends_on: [86, 84]
    description: >
      Add a third option [c] Use gh CLI to the method selection screen.
      New screenGHCLI screen. Verify gh is installed and authenticated.
      On success set token=ghcli sentinel, tokenType=TokenTypeGHCLI.
    testing: >
      Table-driven: select c success, gh not installed, gh not authenticated.
      Existing tests unchanged.

  - id: 93
    title: "Wire gh CLI backend in main.go"
    status: done
    depends_on: [87, 88, 89, 90, 91, 92]
    description: >
      Update runTUI to branch on token_type. Create ghcli.ExecRunner, verify auth,
      create ghcli fetchers, mutator, diff fetcher, rate limit reader.
      Set poll interval to max(cfg.PollInterval, 30s) for search API limits.
    testing: >
      Test runTUI with token_type=ghcli using stubbed gh dependencies.
      Test authenticate fallback to gh verifier. Existing API backend tests unchanged.

  # ─── Phase 20: gh CLI Backend Diagnostics ─────────────────────────────────

  - id: 94
    title: "Add debug logging to ghcli runner and fetchers"
    status: done
    depends_on: [93]
    description: >
      Add slog.Debug / slog.Error calls throughout the ghcli package so all
      commands, outputs, and results are visible in the debug log.
      runner.go: log command args before execution, output byte count on success, full stderr on error.
      auth.go: log the extracted login.
      my_prs.go: log PR count after JSON parse.
      review_queue.go: log PR count after JSON parse.
    testing: >
      slog.Debug calls are side-effect-only with no branching, so existing tests
      already cover these paths. No new test branches needed.

  - id: 95
    title: "Remove hardcoded --hostname github.com from auth verifier"
    status: done
    depends_on: [93]
    description: >
      Change auth.go Verify() args from ["auth", "status", "--hostname", "github.com"]
      to ["auth", "status"]. Running gh auth status without --hostname reports all
      authenticated hosts. parseGHAuthLogin already handles this generically.
      Update auth_test.go stub expectations and add test case for multi-host output.
    testing: >
      Update stub expectation from [auth status --hostname github.com] to [auth status].
      Add test case for multi-host output format (two hosts, picks first account line).

  # ─── Phase 21: Poller Initial Fetch Fix ──────────────────────────────────────

  - id: 96
    title: "Add initial fetch to Poller.run()"
    status: done
    depends_on: [12]
    description: >
      Add _ = p.fetch(ctx) immediately before the for loop in run(), after
      ticker creation. This ensures the UI has data immediately on startup
      rather than waiting for the first tick (30s for ghcli backend).
    testing: >
      Add TestPoller_InitialFetch_CalledOnStart. Verify both fetchers are called
      before any tick fires.

  - id: 97
    title: "Update affected poller tests for initial fetch"
    status: done
    depends_on: [96]
    description: >
      TestPoller_PausedWhenRateLimitCritical_FetchNotCalled: drain initial fetch
      call before asserting ticks don't trigger additional fetches.
      TestPoller_FetchError_PollerContinues: initial fetch consumes firstCall=true
      error case; adjust so test expects initial fetch error then tick success.
    testing: >
      All existing poller tests pass with the initial fetch change.

  - id: 98
    title: "Add poller lifecycle logging"
    status: done

  - id: 100
    title: "Exclude current user's own PRs from Review Queue panel"
    status: done
    depends_on: [18]
    description: >
      In ReviewQueuePanel.refresh(), skip PRs where pr.Author matches p.username.
      The pull_requests table is shared between My PRs and Review Queue panels, so
      filtering must happen in the UI layer. Add a guard: if p.username != "" &&
      pr.Author == p.username { continue } at the top of the PR iteration loop.
    testing: >
      Table-driven TestReviewQueuePanel_ExcludesOwnPRs with cases:
      - Excludes PR authored by current user (mix of own + others).
      - Includes all PRs when none are by current user.
      - Excludes all PRs when all are by current user (empty panel).
      - Empty username skips filtering (all PRs shown).

  - id: 99
    title: "Fix ghcli fetchers: two-phase fetch (search + pr view)"
    status: done
    depends_on: [87, 88]
    description: >
      gh search prs does not support statusCheckRollup, reviews, reviewRequests,
      or commits as JSON fields. Split into two phases: (1) gh search prs with
      only supported fields for the PR list, (2) gh pr view per PR for detail
      fields. Remove unsupported fields from ghSearchPR struct. Add ghPRDetail
      struct and fetchPRDetail helper. Remove ghSearchRQPR (no longer needed).
      On detail fetch error, persist with empty detail (graceful degradation).
    testing: >
      Update StubCommandRunner usage to dispatch search vs view responses.
      Update test fixtures. Add detail fetch error test. Add detail malformed
      JSON test. All existing assertions preserved. 100% coverage maintained.
    depends_on: [96]
    description: >
      Add slog.Debug calls for poller lifecycle events: starting (with interval),
      initial fetch complete, tick fetch, force poll, stopped.
    testing: >
      slog.Debug calls are side-effect-only. Existing tests cover all code paths.

  # ─── Phase 22: PR Panel Table Layout ───────────────────────────────────────

  - id: 101
    title: "Add RowCounter interface and include count in panel title"
    status: done
    depends_on: [17, 18]
    description: >
      Add RowCounter optional interface with RowCount() int to model.go.
      Modify panelView() to include count in title via type assertion.
      Update model_test.go stubs and title assertions.
    testing: >
      stubSubModel implements RowCount() returning 0; TestView_ContainsAllPanelRegions
      assertions updated to match "MY PULL REQUESTS [0]" and "REVIEW QUEUE [0]".

  - id: 102
    title: "Table layout for My PRs panel"
    status: done
    depends_on: [101]
    description: >
      Add RowCount() to MyPRsPanel. Remove [N open] from View(). Add renderHeader()
      with column headers and separators. Rewrite renderRow() to use fixed-width
      padded columns with flex title. Update panel_prs_test.go accordingly.
    testing: >
      TestMyPRsPanel_RowCount table-driven. TestMyPRsPanel_HeaderRow verifies
      column labels and separators. BadgeCount test updated. Title truncation
      test width adjusted. All existing content tests still pass.

  - id: 103
    title: "Table layout for Review Queue panel"
    status: done
    depends_on: [101]
    description: >
      Add RowCount() to ReviewQueuePanel. Remove [N waiting] from View(). Add
      renderHeader() with column headers and separators. Rewrite renderRow() to
      use fixed-width padded columns with flex title. Update panel_reviews_test.go.
    testing: >
      TestReviewQueuePanel_RowCount table-driven. TestReviewQueuePanel_HeaderRow
      verifies column labels and separators. BadgeCount test updated. Title
      truncation test width adjusted. All existing content tests still pass.

  # ─── Phase 23: Display-Width-Correct Table Layout ───────────────────────────

  - id: 104
    title: "Create shared rowBuilder with display-width-correct layout"
    status: done
    depends_on: [102, 103]
    description: >
      Create internal/ui/tablerow.go with columnDef and rowBuilder types.
      Uses ansi.Truncate for truncation and lipgloss.Width for measurement.
      Manual space-padding to exact display-width. Handles flex columns.
      Create internal/ui/tablerow_test.go with tests for alignment, flex,
      emoji cells, truncation, empty cells, zero-width fallback.
      Promote charmbracelet/x/ansi from indirect to direct in go.mod.
    testing: >
      Table-driven tests for: basic alignment, flex column, emoji cells,
      truncation with ellipsis, empty cells, zero-width fallback.

  - id: 105
    title: "Refactor My PRs panel to use rowBuilder"
    status: done
    depends_on: [104]
    description: >
      Replace renderHeader/renderRow in panel_prs.go with rowBuilder calls.
      Remove titleWidth() method. Define column specs matching current layout.
      Update truncateTitle to use display-width via ansi.Truncate.
      Update TestTruncateTitle with emoji test case.
    testing: >
      All existing panel_prs_test.go tests pass. TestTruncateTitle updated
      for display-width semantics with emoji case added.

  - id: 106
    title: "Refactor Review Queue panel to use rowBuilder"
    status: done
    depends_on: [104]
    description: >
      Replace renderHeader/renderRow in panel_reviews.go with rowBuilder calls.
      Remove titleWidth() method. Define column specs matching current layout.
    testing: >
      All existing panel_reviews_test.go tests pass unchanged.
